# Templates

This directory contains reusable Markdown templates for the vault.

- `BASE_TEMPLATE.md` defines the shared frontmatter and note sections.
- Category-specific `.TEMPLATE.md` files should extend the base template inside each `NN-CategoryName/` directory.
